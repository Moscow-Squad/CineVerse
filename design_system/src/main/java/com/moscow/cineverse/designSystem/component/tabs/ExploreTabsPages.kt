package com.moscow.cineverse.designSystem.component.tabs

import com.moscow.cineverse.design_system.R

enum class ExploreTabsPages(val textId: Int) {
    MOVIES(R.string.movies),
    SERIES(R.string.series),
    ACTORS(R.string.actors)
}